export const authRoutes = [
    "/login"
]

export const adminRoutes = [
    "/admin",
    "/admin/user/register"
]

export const protectedRoutes = [
    "/project"
]

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/project";
export const DEFAULT_UNLOGIN_REDIRECT = "/login";